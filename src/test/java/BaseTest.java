import org.openqa.selenium.WebDriver;
import webdriver.WebDriverFactory;

public abstract class BaseTest {

protected WebDriver driver;

// @before get driver from Factory

    // @after kill driver
}
